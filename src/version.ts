export const MCP_SERVER_VERSION = "0.5.0";

// Change this whenever the public MCP tool schemas change. It is exposed in
// HTTP discovery and each MCP tool's metadata so clients and smoke tests can
// detect a cached catalog independently of the application version.
export const MCP_TOOL_CATALOG_REVISION = "2026-08-01.stonx-allocations";
