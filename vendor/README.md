# Jurisdiction package release candidate

`ekubo-jurisdiction-0.1.0-509e3fc9bce2.tgz` is the generated npm package from
`EkuboProtocol/typescript-sdk`, `packages/jurisdiction`, on
`feat/jurisdiction-package`. The package contains Wasm compiled from the same
Rust crate used by the quoter. Do not edit extracted implementations here.

This artifact keeps the change reproducible before the initial npm release.
After publication, replace the file dependency with the matching npm version.

SHA-256: `509e3fc9bce23b6af840ad81f41689fe56d3d7828c283b2fe22495d87a390dd2`
